package org.example;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Springdoc OpenAPI auto-configures Swagger UI. No need for additional beans.
}
